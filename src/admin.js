function sum(x, y) {
    return x + y;
}

function sub(x, y) {
    return x - y;
}

function murtiply(x, y) {
    return x * y;
}

function devide(x, y) {
    return x / y;
}

console.log(Number.isNaN(parseInt('1212')));